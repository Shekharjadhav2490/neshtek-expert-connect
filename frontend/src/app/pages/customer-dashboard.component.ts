import { Component, inject } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { Router, RouterLink } from '@angular/router';
import { CustomerService, CustomerProfile, CustomerRequirement, CustomerRequirementRequest, CustomerEngagement } from '../core/customer/customer.service';
import { ConsultationRequest, ConsultationRequestService } from '../core/consultation/consultation-request.service';
import { AuthService } from '../core/auth/auth.service';

@Component({
  selector: 'app-customer-dashboard', standalone: true, imports: [CommonModule, RouterLink, FormsModule],
  template: `
    <main class="dashboard"><header class="topbar"><div><div class="brand">NeshTek Expert Connect</div><div class="subtitle">Customer dashboard</div></div><button class="logout" type="button" (click)="logout()">Sign out</button></header>
      <section class="welcome"><div><div class="eyebrow">Customer workspace</div><h1>Welcome back{{ displayName ? ', ' + displayName : '' }}.</h1><p>Manage your company profile, requirements, expert consultations and active engagements from one workspace.</p></div></section>
      @if (loading) { <section class="state-card">Loading your customer profile…</section> } @else if (errorMessage) { <section class="state-card error">{{ errorMessage }}</section> } @else if (customer) {
        <section class="grid"><article class="card profile"><div class="card-title">Company profile</div><div class="profile-name">{{ customer.companyName }}</div><div class="muted">{{ customer.contactName }} · {{ customer.email }}</div><dl><div><dt>Location</dt><dd>{{ customer.city }}, {{ customer.country }}</dd></div><div><dt>Industry</dt><dd>{{ customer.industry || 'Not specified' }}</dd></div><div><dt>Company size</dt><dd>{{ customer.companySize || 'Not specified' }}</dd></div><div><dt>Status</dt><dd>{{ customer.status }}</dd></div></dl></article><article class="card stat"><div class="card-title">Requirements</div><div class="number">{{ requirements.length }}</div><div class="muted">Requirements currently available to your account.</div></article><article class="card next"><div class="card-title">Next step</div><h2>Find expert matches</h2><p>Select a requirement below to compare the best available experts.</p>@if (requirements[0]) { <a class="primary-link" [routerLink]="['/customer/requirements', requirements[0].id, 'matches']">Open latest requirement</a> }</article></section>
        <section class="consultation-section"><div class="section-head"><div><div class="eyebrow">Consultation requests</div><h2>Expert response status</h2><p class="section-description">Track requests sent to experts.</p></div><a class="secondary-link" routerLink="/customer/consultations">View all consultations →</a></div>@if (consultationsLoading) { <section class="state-card compact">Loading consultation requests…</section> } @else if (consultationError) { <section class="state-card error compact">{{ consultationError }}</section> } @else if (!consultations.length) { <section class="state-card compact empty">No consultation requests have been sent yet.</section> } @else { <div class="summary-grid"><div class="summary-card"><span>All</span><b>{{ consultations.length }}</b></div><div class="summary-card"><span>Accepted</span><b>{{ acceptedConsultations }}</b></div><div class="summary-card"><span>Pending</span><b>{{ pendingConsultations }}</b></div><div class="summary-card"><span>Rejected</span><b>{{ rejectedConsultations }}</b></div></div><div class="list">@for (request of recentConsultations; track request.id) { <article class="list-card"><div><div class="label">Requirement</div><h3>{{ request.requirementTitle || ('Requirement #' + request.requirementId) }}</h3><div class="meta">Expert: <b>{{ request.expertName || ('Expert #' + request.expertId) }}</b> · Requested: {{ request.createdAt ? (request.createdAt | date:'medium') : '—' }}</div></div><span class="pill">{{ request.status }}</span></article> }</div> }</section>
        <section class="engagement-section"><div class="section-head"><div><div class="eyebrow">Engagements</div><h2>My engagements</h2></div><button type="button" class="refresh" (click)="loadEngagements()">Refresh</button></div>@if (engagementsLoading) { <section class="state-card compact">Loading engagements…</section> } @else if (engagementError) { <section class="state-card error compact">{{ engagementError }}</section> } @else if (!engagements.length) { <section class="state-card compact empty">No engagements have been created yet.</section> } @else { <div class="summary-grid"><div class="summary-card"><span>All</span><b>{{ engagements.length }}</b></div><div class="summary-card"><span>Ready</span><b>{{ readyEngagements }}</b></div><div class="summary-card"><span>Active</span><b>{{ activeEngagements }}</b></div><div class="summary-card"><span>Completed</span><b>{{ completedEngagements }}</b></div></div><div class="list">@for (engagement of engagements; track engagement.id) { <article class="list-card"><div><div class="label">Engagement #{{ engagement.id }}</div><h3>{{ engagement.requirementTitle || ('Requirement #' + engagement.requirementId) }}</h3><div class="meta">Expert: <b>{{ engagement.expertName || ('Expert #' + engagement.expertId) }}</b> · {{ engagement.estimatedHours }} hrs · {{ engagement.agreedRate }} {{ engagement.currencyCode }}/hr</div></div><div class="right"><span class="pill">{{ engagement.status }}</span><a [routerLink]="['/customer/work-logs', engagement.id]">View work logs →</a></div></article> }</div> }</section>
        <section class="requirements-section"><div class="section-head"><div><div class="eyebrow">Your requirements</div><h2>Requirements & expert matching</h2></div><button class="primary" type="button" (click)="openCreate()">+ Create requirement</button></div>@if (successMessage) { <div class="notice success">{{ successMessage }}</div> } @if (requirementsLoading) { <section class="state-card">Loading requirements…</section> } @else if (!requirements.length) { <section class="state-card empty">No requirements are available yet. Create your first requirement.</section> } @else { <div class="requirements-list">@for (requirement of requirements; track requirement.id) { <article class="requirement-card"><div class="requirement-main"><div class="requirement-title">{{ requirement.title }}</div><div class="muted">{{ requirement.technology || 'Technology not specified' }} · {{ requirement.requiredExperienceYears || 0 }} years+ · {{ requirement.priority }} · {{ requirement.status }}</div><div class="skill-list">@for (skill of requirement.skills; track skill.id) { <span>{{ skill.skillName }}</span> }</div></div><div class="row-actions"><a class="edit" [routerLink]="['/customer/requirements', requirement.id, 'edit']">Edit</a><a class="match-link" [routerLink]="['/customer/requirements', requirement.id, 'matches']">Find expert matches →</a>@if (canDelete(requirement)) { <button class="delete" type="button" (click)="deleteRequirement(requirement)">Delete</button> }</div></article> }</div> }</section>
      }
      @if (showCreate && customer) { <div class="overlay"><section class="modal"><div class="modal-head"><div><div class="eyebrow">New requirement</div><h2>Create a requirement</h2></div><button class="icon" type="button" (click)="closeCreate()">×</button></div><form (ngSubmit)="createRequirement()"><div class="form-grid"><label>Title *<input name="title" [(ngModel)]="form.title" required></label><label>Technology<input name="technology" [(ngModel)]="form.technology"></label><label class="wide">Description *<textarea name="description" [(ngModel)]="form.description" required rows="4"></textarea></label><label>Experience<input type="number" name="experience" [(ngModel)]="form.requiredExperienceYears"></label><label>Estimated hours<input type="number" name="hours" [(ngModel)]="form.estimatedHours"></label><label>Preferred start date<input type="date" name="startDate" [(ngModel)]="form.preferredStartDate"></label><label>Priority<select name="priority" [(ngModel)]="form.priority"><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>URGENT</option></select></label><label>Budget<input type="number" name="budget" [(ngModel)]="form.budget"></label><label>Currency<select name="currency" [(ngModel)]="form.currencyCode"><option>USD</option><option>INR</option><option>EUR</option><option>GBP</option><option>AED</option></select></label><label class="wide">Skills<input name="skills" [(ngModel)]="skillsText"></label></div>@if (formError) { <div class="notice error-box">{{ formError }}</div> }<div class="actions"><button type="button" class="secondary" (click)="closeCreate()">Cancel</button><button type="submit" class="primary" [disabled]="saving">{{ saving ? 'Creating…' : 'Create requirement' }}</button></div></form></section></div> }
    </main>`,
  styles: [`
    .dashboard{min-height:100vh;background:#f7f9fc;color:#172033;font-family:Inter,system-ui,sans-serif;padding:0 32px 48px}.topbar,.welcome,.grid,.consultation-section,.engagement-section,.requirements-section{max-width:1180px;margin-left:auto;margin-right:auto}.topbar{padding:22px 0;border-bottom:1px solid #e3e8f0;display:flex;justify-content:space-between}.brand{font-weight:850}.subtitle,.muted{color:#667085;font-size:14px}.welcome{padding:52px 0 25px}.eyebrow{font-size:11px;font-weight:850;letter-spacing:.12em;text-transform:uppercase;color:#315ea8}.welcome h1{font-size:42px;margin:9px 0}.welcome p,.section-description{color:#667085}.grid{display:grid;grid-template-columns:2fr 1fr 1fr;gap:18px}.card,.state-card,.requirement-card,.list-card{background:#fff;border:1px solid #e1e6ef;border-radius:16px;padding:22px;box-shadow:0 10px 28px rgba(23,32,51,.04)}.profile-name{font-size:24px;font-weight:850;margin:16px 0 5px}.profile dl{display:grid;grid-template-columns:1fr 1fr;gap:15px}.profile dt{font-size:12px;color:#98a2b3}.profile dd{font-weight:700;margin:4px 0 0}.number{font-size:44px;font-weight:850;margin:25px 0 4px}.section-head{display:flex;justify-content:space-between;align-items:end;margin-bottom:15px}.consultation-section,.engagement-section,.requirements-section{margin-top:38px}.summary-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:12px}.summary-card{background:#fff;border:1px solid #e1e6ef;border-radius:11px;padding:14px 16px;display:flex;justify-content:space-between}.summary-card span{color:#667085;font-size:12px}.summary-card b{font-size:22px}.list{display:grid;gap:10px}.list-card{display:flex;justify-content:space-between;align-items:center;gap:20px}.list-card h3{margin:6px 0;font-size:17px}.meta{color:#667085;font-size:12px}.right{display:flex;flex-direction:column;align-items:flex-end;gap:7px}.pill{background:#eef2f7;border-radius:999px;padding:7px 11px;font-size:11px;font-weight:850}.requirements-list{display:grid;gap:12px}.requirement-card{display:flex;justify-content:space-between;align-items:center;gap:20px}.requirement-title{font-size:18px;font-weight:850}.skill-list{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px}.skill-list span{background:#eef2f7;border-radius:999px;padding:6px 9px;font-size:11px}.row-actions{display:flex;align-items:center;gap:14px;white-space:nowrap}.row-actions a{text-decoration:none;font-weight:800}.edit{color:#172033;border:1px solid #d5dbe6;background:#fff;padding:9px 13px;border-radius:8px}.match-link{color:#315ea8}.delete{border:0;background:transparent;color:#b42318;font-weight:800;cursor:pointer}.primary,.secondary,.logout,.refresh,.icon{border-radius:9px;padding:10px 15px;font-weight:800;cursor:pointer}.primary{border:0;background:#172033;color:#fff}.secondary,.logout,.refresh,.icon{border:1px solid #d5dbe6;background:#fff;color:#172033}.notice{padding:12px 14px;border-radius:9px;margin:12px 0}.success{background:#ecfdf3;color:#067647}.error-box{background:#fff5f4;color:#b42318}.overlay{position:fixed;inset:0;background:rgba(23,32,51,.35);display:flex;align-items:center;justify-content:center;padding:20px;z-index:10}.modal{background:#fff;border-radius:16px;max-width:850px;width:100%;max-height:90vh;overflow:auto;padding:26px}.modal-head{display:flex;justify-content:space-between}.icon{font-size:22px}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.form-grid label{display:flex;flex-direction:column;gap:6px;font-size:12px;font-weight:800}.wide{grid-column:1/-1}.form-grid input,.form-grid textarea,.form-grid select{font:inherit;padding:10px;border:1px solid #d5dbe6;border-radius:8px}.actions{display:flex;justify-content:flex-end;gap:10px;margin-top:20px}.state-card.error{color:#b42318;background:#fff5f4}@media(max-width:800px){.dashboard{padding:0 18px 40px}.grid,.form-grid{grid-template-columns:1fr}.requirement-card,.list-card{align-items:flex-start;flex-direction:column}.row-actions{flex-wrap:wrap}.summary-grid{grid-template-columns:1fr 1fr}.wide{grid-column:auto}}
  `]
})
export class CustomerDashboardComponent {
  private readonly customerService=inject(CustomerService); private readonly consultationService=inject(ConsultationRequestService); private readonly auth=inject(AuthService); private readonly router=inject(Router);
  customer:CustomerProfile|null=null; requirements:CustomerRequirement[]=[]; consultations:ConsultationRequest[]=[]; engagements:CustomerEngagement[]=[]; loading=true; requirementsLoading=true; consultationsLoading=true; engagementsLoading=true; errorMessage=''; consultationError=''; engagementError=''; successMessage=''; showCreate=false; saving=false; formError=''; skillsText='';
  form:Partial<CustomerRequirementRequest>={title:'',description:'',technology:'',requiredExperienceYears:0,estimatedHours:1,preferredStartDate:'',priority:'MEDIUM',budget:0,currencyCode:'USD'};
  get displayName(){return this.customer?.contactName??'';} get acceptedConsultations(){return this.consultations.filter(r=>r.status==='ACCEPTED').length;} get pendingConsultations(){return this.consultations.filter(r=>r.status==='PENDING').length;} get rejectedConsultations(){return this.consultations.filter(r=>r.status==='REJECTED').length;} get recentConsultations(){return this.consultations.slice(0,3);} get readyEngagements(){return this.engagements.filter(e=>e.status==='READY').length;} get activeEngagements(){return this.engagements.filter(e=>e.status==='ACTIVE').length;} get completedEngagements(){return this.engagements.filter(e=>e.status==='COMPLETED').length;}
  constructor(){const user=this.auth.getCurrentUser();if(!user){this.router.navigateByUrl('/login');return;}this.loadCustomer(user.userId);this.loadConsultations(user.userId);}
  private loadCustomer(userId:number){this.customerService.getCustomer(userId).subscribe({next:c=>{this.customer=c;this.loading=false;this.loadRequirements();this.loadEngagements();},error:e=>{this.loading=false;this.errorMessage=e?.error?.error||'Unable to load your customer profile.';}});}
  private loadRequirements(){if(!this.customer)return;this.customerService.getRequirements(this.customer.customerId).subscribe({next:p=>{this.requirements=p.content??[];this.requirementsLoading=false;},error:()=>{this.requirements=[];this.requirementsLoading=false;}});}
  private loadConsultations(userId:number){this.consultationService.listMine(userId).subscribe({next:p=>{this.consultations=p.content??[];this.consultationsLoading=false;},error:e=>{this.consultations=[];this.consultationsLoading=false;this.consultationError=e?.error?.message||e?.error?.error||'Unable to load consultation requests.';}});}
  loadEngagements(){if(!this.customer)return;this.customerService.getEngagements(this.customer.customerId).subscribe({next:p=>{this.engagements=p.content??[];this.engagementsLoading=false;},error:e=>{this.engagements=[];this.engagementsLoading=false;this.engagementError=e?.error?.message||e?.error?.error||'Unable to load engagements.';}});}
  openCreate(){this.successMessage='';this.formError='';this.skillsText='';this.form={title:'',description:'',technology:'',requiredExperienceYears:0,estimatedHours:1,preferredStartDate:'',priority:'MEDIUM',budget:0,currencyCode:'USD'};this.showCreate=true;} closeCreate(){if(!this.saving)this.showCreate=false;}
  createRequirement(){if(!this.customer||this.saving)return;this.formError='';if(!this.form.title?.trim()||!this.form.description?.trim()){this.formError='Title and description are required.';return;}const request:CustomerRequirementRequest={customerId:this.customer.customerId,companyName:this.customer.companyName,contactName:this.customer.contactName,email:this.customer.email,phone:this.customer.phone,country:this.customer.country,city:this.customer.city,title:this.form.title.trim(),description:this.form.description.trim(),technology:this.form.technology?.trim(),requiredExperienceYears:Number(this.form.requiredExperienceYears||0),estimatedHours:Number(this.form.estimatedHours||1),preferredStartDate:this.form.preferredStartDate||undefined,priority:this.form.priority||'MEDIUM',budget:Number(this.form.budget||0),currencyCode:this.form.currencyCode||'USD',skills:this.skillsText.split(',').map(s=>s.trim()).filter(Boolean).map((skillName,i)=>({skillName,priorityOrder:i+1,mandatory:true}))};this.saving=true;this.customerService.createRequirement(request).subscribe({next:()=>{this.saving=false;this.showCreate=false;this.successMessage='Requirement created successfully.';this.loadRequirements();},error:e=>{this.saving=false;this.formError=e?.error?.message||e?.error?.error||'Unable to create requirement.';}});}
  canDelete(r:CustomerRequirement){return r.status==='DRAFT'||r.status==='SUBMITTED';} deleteRequirement(r:CustomerRequirement){if(!this.canDelete(r)||!confirm('Delete requirement "'+r.title+'"?'))return;this.customerService.deleteRequirement(r.id).subscribe({next:()=>{this.successMessage='Requirement deleted successfully.';this.loadRequirements();},error:e=>{this.errorMessage=e?.error?.message||e?.error?.error||'Unable to delete requirement.';}});} logout(){this.auth.logout();this.router.navigateByUrl('/login');}
}
