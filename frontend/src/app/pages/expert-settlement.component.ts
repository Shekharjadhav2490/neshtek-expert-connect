import { Component, inject } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterLink } from '@angular/router';
import { forkJoin } from 'rxjs';
import { AuthService } from '../core/auth/auth.service';
import { Engagement, ExpertProfile, ExpertService } from '../core/expert/expert.service';
import { EngagementBillingSummary, WorkLogService } from '../core/work-log/work-log.service';
import { Settlement, SettlementService } from '../core/settlement/settlement.service';

@Component({selector:'app-expert-settlement',standalone:true,imports:[CommonModule,RouterLink],template:`
<main class="page"><header class="topbar"><div><b>NeshTek Expert Connect</b><div class="sub">Expert earnings & settlement</div></div><a routerLink="/expert/dashboard">Expert dashboard</a></header>
<section class="content"><div class="eyebrow">Billing & settlement</div><h1>Earnings & settlement</h1><p class="intro">Track approved earnings and request settlement. Approved requests move to admin review before payout.</p>
@if(error){<div class="state error">{{error}}</div>}
@if(loading){<div class="state">Loading your settlement summary…</div>}
@if(!loading && expert){
<section class="summary"><article><span>Approved earnings</span><b>{{totalApproved | number:'1.0-2'}} {{currency}}</b><small>Customer-approved work</small></article><article><span>Pending approval</span><b>{{totalPending | number:'1.0-2'}} {{currency}}</b><small>Submitted but not approved</small></article><article><span>Rejected value</span><b>{{totalRejected | number:'1.0-2'}} {{currency}}</b><small>Excluded from settlement</small></article><article><span>Approved hours</span><b>{{totalApprovedHours | number:'1.0-2'}} hrs</b><small>Across all engagements</small></article></section>
<section class="card"><div class="section-head"><div><div class="eyebrow">Settlement lifecycle</div><h2>Request → approve → pay</h2></div><button (click)="load()">Refresh</button></div><div class="steps"><span class="active">1. Request settlement</span><span>2. Admin approval</span><span>3. Payment recorded</span></div><div class="note">No payment is initiated automatically. The payment reference is recorded by an administrator after the actual payout is completed.</div></section>
<section class="settlements"><div class="section-head"><div><div class="eyebrow">Settlement history</div><h2>My settlements</h2></div></div>@if(!settlements.length){<div class="state">No settlement requests yet.</div>}@else{@for(s of settlements;track s.id){<article class="card settlement-row"><div><h3>{{s.requirementTitle}}</h3><div class="muted">Engagement #{{s.engagementId}} · Requested {{s.requestedAt | date:'medium'}}</div><div class="meta"><span>{{s.approvedHours}} hrs</span><span>{{s.hourlyRate}} {{s.currencyCode}}/hr</span><span>{{s.grossAmount}} {{s.currencyCode}}</span></div>@if(s.adminComment){<div class="comment">{{s.adminComment}}</div>}</div><div class="amount"><span class="badge" [class.requested]="s.status==='REQUESTED'" [class.approved]="s.status==='APPROVED_FOR_PAYOUT'" [class.paid]="s.status==='PAID'" [class.rejected]="s.status==='REJECTED'">{{label(s.status)}}</span>@if(s.paymentReference){<small>Ref: {{s.paymentReference}}</small>}</div></article>}}</section>
<section class="engagements"><div class="eyebrow">Eligible earnings</div><h2>Engagement earnings</h2>@if(!rows.length){<div class="state">No engagement billing records found.</div>}@else{@for(row of rows;track row.summary.engagementId){<article class="card row"><div class="main"><h3>{{row.engagement.requirementTitle}}</h3><div class="muted">Customer #{{row.engagement.customerId}} · Engagement #{{row.engagement.id}}</div><div class="meta"><span>{{row.summary.approvedHours}} approved hrs</span><span>{{row.summary.pendingHours}} pending hrs</span><span>{{row.summary.rejectedHours}} rejected hrs</span><span>{{row.summary.hourlyRate ?? row.engagement.agreedRate ?? '—'}} {{row.summary.currencyCode ?? row.engagement.currencyCode ?? ''}}/hr</span></div></div><div class="amount"><span>Approved earnings</span><b>{{(row.summary.approvedBilling ?? 0) | number:'1.0-2'}} {{row.summary.currencyCode ?? ''}}</b>@if(canRequest(row.engagement.id,row.summary.approvedBilling)){<button class="request" [disabled]="actionId===row.engagement.id" (click)="requestSettlement(row.engagement.id)">{{actionId===row.engagement.id?'Requesting…':'Request settlement'}}</button>}@else if(openSettlement(row.engagement.id)){<small>Settlement {{label(openSettlement(row.engagement.id)!.status)}}</small>}<a [routerLink]="['/expert/work-logs',row.engagement.id]">View work logs →</a></div></article>}}</section>}
</section></main>`,styles:[`.page{min-height:100vh;background:#f7f9fc;color:#172033;font-family:Inter,system-ui,sans-serif;padding:0 32px 56px;box-sizing:border-box}.topbar,.content{max-width:1180px;margin:auto}.topbar{padding:22px 0;border-bottom:1px solid #e3e8f0;display:flex;justify-content:space-between;align-items:center}.sub,.muted,.intro{color:#667085;font-size:14px}.sub{margin-top:4px}.topbar a{border:1px solid #d5dbe6;background:#fff;border-radius:9px;padding:10px 14px;text-decoration:none;color:#315ea8;font-weight:750}.content{padding-top:46px}.eyebrow{text-transform:uppercase;letter-spacing:.1em;font-size:11px;font-weight:850;color:#315ea8}.content h1{font-size:42px;margin:9px 0}.intro{line-height:1.6;max-width:800px}.summary{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-top:28px}.summary article,.card,.state{background:#fff;border:1px solid #e1e6ef;border-radius:16px;padding:22px;box-shadow:0 12px 32px rgba(23,32,51,.05)}.summary span,.amount>span{display:block;color:#98a2b3;text-transform:uppercase;font-size:11px;margin-bottom:7px}.summary b{font-size:24px}.summary small{display:block;color:#667085;margin-top:6px}.card{margin-top:22px}.section-head{display:flex;justify-content:space-between;align-items:flex-start}.section-head h2,.engagements h2,.settlements h2{margin:5px 0 0}.section-head button{border:1px solid #d5dbe6;background:#fff;border-radius:9px;padding:9px 14px;font-weight:750}.steps{display:flex;gap:10px;flex-wrap:wrap;margin-top:20px}.steps span{padding:9px 12px;border-radius:999px;background:#f2f4f7;color:#667085;font-size:12px;font-weight:800}.steps span.active{background:#eef4ff;color:#315ea8}.note{margin-top:16px;padding:12px 14px;background:#f8fafc;border-radius:9px;color:#667085;font-size:13px}.settlements,.engagements{margin-top:38px}.settlement-row,.row{display:flex;justify-content:space-between;gap:24px;align-items:center}.settlement-row h3,.row h3{margin:0 0 5px;font-size:19px}.meta{display:flex;gap:14px;flex-wrap:wrap;margin-top:14px;color:#536078;font-size:13px;font-weight:700}.comment{margin-top:12px;color:#667085;font-size:13px;background:#f8fafc;padding:10px;border-radius:8px}.amount{text-align:right;min-width:190px}.amount b{display:block;font-size:20px;margin-bottom:9px}.amount a{display:block;color:#315ea8;font-size:12px;font-weight:800;text-decoration:none;margin-top:9px}.amount small{display:block;color:#667085;margin-top:7px}.badge{display:inline-block;padding:6px 10px;border-radius:999px;font-size:11px;font-weight:850}.badge.requested{background:#fff4d6;color:#8a5a00}.badge.approved{background:#eef4ff;color:#315ea8}.badge.paid{background:#ecfdf3;color:#067647}.badge.rejected{background:#fff5f4;color:#b42318}.request{border:0;border-radius:9px;padding:9px 13px;background:#172033;color:#fff;font-weight:800;cursor:pointer}.request:disabled{opacity:.5;cursor:not-allowed}.error{color:#b42318;background:#fff5f4}.state{color:#667085;margin-top:22px}.main{flex:1}@media(max-width:900px){.summary{grid-template-columns:repeat(2,1fr)}.row,.settlement-row{flex-direction:column;align-items:flex-start}.amount{text-align:left}.amount a{display:block}}@media(max-width:600px){.page{padding:0 18px 40px}.summary{grid-template-columns:1fr}.content h1{font-size:34px}}`]
})
export class ExpertSettlementComponent {
 private readonly auth=inject(AuthService); private readonly expertService=inject(ExpertService); private readonly workLogService=inject(WorkLogService); private readonly settlementService=inject(SettlementService);
 expert:ExpertProfile|null=null; rows:{engagement:Engagement;summary:EngagementBillingSummary}[]=[]; settlements:Settlement[]=[]; loading=true; error=''; actionId:number|null=null; totalApproved=0; totalPending=0; totalRejected=0; totalApprovedHours=0; currency='INR';
 constructor(){this.load()}
 load(){const user=this.auth.getCurrentUser();if(!user){this.error='Please sign in as an expert.';this.loading=false;return}this.loading=true;this.error='';this.expertService.findByEmail(user.email).subscribe({next:expert=>{this.expert=expert;forkJoin({engagements:this.expertService.getEngagements(expert.expertId),settlements:this.settlementService.byExpert(expert.expertId)}).subscribe({next:r=>{const engagements=r.engagements.content??[];this.settlements=r.settlements.content??[];if(!engagements.length){this.rows=[];this.finish();return}forkJoin(engagements.map(e=>this.workLogService.getBillingSummary(e.id))).subscribe({next:summaries=>{this.rows=engagements.map((engagement,i)=>({engagement,summary:summaries[i]}));this.totalApproved=this.rows.reduce((s,r)=>s+(r.summary.approvedBilling??0),0);this.totalPending=this.rows.reduce((s,r)=>s+(r.summary.pendingBilling??0),0);this.totalRejected=this.rows.reduce((s,r)=>s+(r.summary.rejectedValue??0),0);this.totalApprovedHours=this.rows.reduce((s,r)=>s+r.summary.approvedHours,0);this.currency=this.rows.find(r=>r.summary.currencyCode)?.summary.currencyCode??'INR';this.finish()},error:e=>{this.error=e?.error?.error||'Unable to load billing summaries.';this.finish()}})},error:e=>{this.error=e?.error?.error||'Unable to load settlement data.';this.finish()}})},error:e=>{this.error=e?.error?.error||e?.message||'Unable to load your expert profile.';this.finish()}})}
 openSettlement(id:number):Settlement|undefined{return this.settlements.find(s=>s.engagementId===id && (s.status==='REQUESTED'||s.status==='APPROVED_FOR_PAYOUT'));}
 canRequest(id:number,approved:number|null):boolean{return (approved??0)>0&&!this.openSettlement(id)&&this.settlements.find(s=>s.engagementId===id)?.status!=='PAID';}
 requestSettlement(id:number){this.actionId=id;this.settlementService.request(id).subscribe({next:s=>{this.settlements=[s,...this.settlements];this.actionId=null;},error:e=>{this.error=e?.error?.error||'Unable to request settlement.';this.actionId=null;}})}
 label(status:string):string{return status==='APPROVED_FOR_PAYOUT'?'Approved for payout':status.charAt(0)+status.slice(1).toLowerCase()}
 private finish(){this.loading=false}
}
